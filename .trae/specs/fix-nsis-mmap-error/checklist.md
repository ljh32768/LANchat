- [ ] `release/` 目录、NSIS 缓存目录 `C:\Users\admin\AppData\Local\electron-builder\Cache\nsis-3.0.4.1\`、`%TEMP%\ns*.tmp` 临时文件均已删除
- [ ] `Get-MpPreference` 输出中包含四个排除路径：项目目录、electron-builder 缓存、NSIS 缓存、临时目录
- [ ] `package.json` 的 `build.nsis` 包含 `uninstallDisplayName: "LANChatroom 1.0.0"` 与 `compression: "maximum"`
- [ ] `package.json` 的 `scripts` 包含 `dist:nsis:clean`
- [ ] `node -e "require('./package.json')"` 执行无异常（JSON 合法）
- [ ] `npm run dist:nsis:clean` 执行完成且无 `error creating mmap` 错误
- [ ] `release\LANChatroom Setup 1.0.0.exe` 文件存在
- [ ] 安装包运行后显示中文安装向导，提供"为当前用户/所有用户安装"两个可选选项
- [ ] 安装完成后应用可正常启动，窗口正常显示无白屏
- [ ] 应用运行后 `C:\Users\admin\AppData\Roaming\LANChatroom\chat.db` 文件被创建（打包环境 userData 路径正确）
- [ ] 若采用降级方案（`compression: "store"`），上述检查项同样全部通过
